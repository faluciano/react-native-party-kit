{
  "name": "@party-kit/core",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "test": "bun test",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "eslint": "^8.57.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
